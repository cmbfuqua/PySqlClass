-- DDL Basics Assignment

/*
  Problem Statement:
  You are tasked with designing a simple database schema for a library system.
  
  Instructions:
  1. Create a table named 'books'.
     It must have the following columns:
     - 'book_id' (INTEGER PRIMARY KEY)
     - 'title' (TEXT)
     - 'author' (TEXT)
     - 'published_year' (INTEGER)
  
  2. Create a table named 'members'.
     It must have the following columns:
     - 'member_id' (INTEGER PRIMARY KEY)
     - 'first_name' (TEXT)
     - 'last_name' (TEXT)
     - 'join_date' (TEXT)
     
  3. Alter the 'books' table to add a 'genre' column of type TEXT.
  
  4. Finally, drop the 'members' table (assume the library is closing its membership program).
*/

-- Write your SQL statements below:
