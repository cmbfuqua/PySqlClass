-- DDL Basics Practice

-- Example: Creating a simple table
CREATE TABLE example_table (
    id INTEGER PRIMARY KEY,
    name TEXT
);

-- Example: Altering a table
ALTER TABLE example_table ADD COLUMN description TEXT;

-- Example: Dropping a table
DROP TABLE example_table;

-- Exercise 1: Create a table named 'students'
-- It should have an 'id' (INTEGER PRIMARY KEY), 'first_name' (TEXT), 'last_name' (TEXT), and 'age' (INTEGER).
-- TODO: Write the CREATE TABLE statement below
CREATE TABLE students (
    id INTEGER PRIMARY KEY,
    -- TODO: Add the remaining columns
);

-- Exercise 2: Alter the 'students' table to add a 'grade' column of type TEXT.
-- TODO: Write the ALTER TABLE statement below


-- Exercise 3: Drop the 'students' table.
-- TODO: Write the DROP TABLE statement below
