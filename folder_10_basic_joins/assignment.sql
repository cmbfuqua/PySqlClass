-- ASSIGNMENT: Basic SQL Joins
-- You are given two tables: `customers` (customer_id, customer_name) and `orders` (order_id, customer_id, order_date, total_amount).

-- Task 1:
-- Create a table named `assignment_result_1` that contains the `customer_name` and `total_amount` for all customers who have placed an order. 
-- Use an INNER JOIN.
-- TODO: Write the CREATE TABLE ... AS SELECT statement for Task 1


-- Task 2:
-- Create a table named `assignment_result_2` that contains ALL `customer_name`s, and the `order_id` if they have placed an order. 
-- If they haven't placed an order, `order_id` should be NULL.
-- Use a LEFT JOIN.
-- TODO: Write the CREATE TABLE ... AS SELECT statement for Task 2
