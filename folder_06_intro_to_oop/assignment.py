"""
Assignment: Library Management System

1. Create a class `Book` with attributes `title`, `author`, and `is_checked_out` (which defaults to False).
   It should have the following methods:
   - `checkout()`: Sets `is_checked_out` to True. Returns True if successful, False if it was already checked out.
   - `return_book()`: Sets `is_checked_out` to False. Returns True if successful, False if it was already returned (not checked out).

2. Create a class `Library` that manages a collection of Book objects.
   The __init__ method should initialize an empty list called `books`.
   It should have the following methods:
   - `add_book(book)`: Takes a Book object and adds it to the library's list of books.
   - `get_available_books()`: Returns a list of titles (strings) of books that are NOT checked out.
"""

class Book:
    # TODO: Implement the Book class
    pass

class Library:
    # TODO: Implement the Library class
    pass
