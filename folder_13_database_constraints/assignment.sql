-- Assignment: Database Constraints
-- In this assignment, you will build a table from scratch to track library members.

-- TODO: Create a table named 'LibraryMembers' with the following specifications:
-- 1. 'member_id' of type INTEGER, serving as the PRIMARY KEY.
-- 2. 'phone_number' of type VARCHAR(15), must be UNIQUE.
-- 3. 'full_name' of type VARCHAR(100), cannot be NULL.
-- 4. 'join_date' of type DATE, defaults to '2020-01-01'.
-- 5. 'books_borrowed' of type INTEGER, must be >= 0 (CHECK constraint).

