# Control Flow and Conditionals - Assignment

def get_ticket_price(age, is_student):
    """
    Problem:
    Calculate the ticket price for a movie based on age and student status.
    - Base price is $15.
    - Seniors (65 and older) get a $5 discount (price: $10).
    - Children (under 12) pay $8.
    - Students (any age) get a $3 discount off the base price (price: $12), 
      but discounts cannot be combined. 
      Apply the BEST possible price for the customer.
      
    Example:
    get_ticket_price(70, False) -> 10
    get_ticket_price(20, True) -> 12
    get_ticket_price(10, True) -> 8
    get_ticket_price(30, False) -> 15
    """
    # Write your code here
    pass
